<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Haber Yönetimi | NewsHub</title>
    <%- include('../partials/bootstrap') %>
  </head>
  <body style="background-color: #f3f2ec">
    <div class="container-fluid">
      <div class="row flex-nowrap">
        <!-- Sidebar -->
        <%- include('../partials/sidebar') %>

        <!-- Main Content -->
        <div class="col py-4">
          <h2 class="mb-3" style="color: #1e93ab">Haber Yönetimi</h2>
          <p class="text-muted">Sistemdeki tüm haberlerin listesi.</p>

          <div class="card shadow-sm mt-4">
            <div class="card-header bg-danger text-white">
              <h4 class="mb-0">Haberler</h4>
            </div>
            <div class="card-body p-0">
              <table class="table table-hover mb-0">
                <thead class="bg-danger text-white">
                  <tr>
                    <th>Başlık</th>
                    <th>Yazar</th>
                    <th>Kategori</th>
                    <th>Tarih</th>
                    <th>Aksiyon</th>
                  </tr>
                </thead>
                <tbody>
                  <% if (allPosts && allPosts.length > 0) { %> <%
                  allPosts.forEach(post => { %>
                  <tr>
                    <td><%= post.title %></td>
                    <td>
                      <%= post.author ? post.author.name : 'Bilinmiyor' %>
                    </td>
                    <td><%= post.category ? post.category.name : 'Yok' %></td>
                    <td>
                      <%= new Date(post.createdAt).toLocaleDateString('tr-TR', {
                      day: '2-digit', month: 'short', year: 'numeric' }) %>
                    </td>
                    <td>
                      <a
                        href="/admin/posts/update/<%= post._id %>"
                        class="btn btn-warning btn-sm me-2"
                      >
                        <i class="bi bi-pencil"></i> Düzenle
                      </a>
                      <a
                        href="/admin/posts/delete/<%= post._id %>"
                        class="btn btn-danger btn-sm"
                        onclick="return confirm('Bu haberi silmek istediğinizden emin misiniz?');"
                      >
                        <i class="bi bi-trash"></i> Sil
                      </a>
                    </td>
                  </tr>
                  <% }) %> <% } else { %>
                  <tr>
                    <td colspan="5" class="text-center text-muted">
                      Sistemde yayınlanmış hiç haber bulunmamaktadır.
                    </td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
